<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.henry.ighubchurchapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> -->
    <!-- <uses-permission android:name="android.permission.CAMERA" /> -->

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomePage" />
        <activity android:name=".MembersRegistration" />
        <activity android:name=".PastorRegistration" />
        <activity android:name=".AdminRegistration" />
        <activity android:name=".MemberLoginPage" />
        <activity android:name=".PastorLoginPage" />

        <activity android:name=".PastorPage" />
        <activity android:name=".LoginPage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".VerifyPhoneActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".VideoStreeming"/>
        <activity android:name=".notespad.NoteEdit" />
        <activity android:name=".notespad.NotesPad" />
        <activity android:name=".video.VideoAppMainActivity"/>

    </application>

</manifest>